# nixpacks.toml - Configure system libraries for Playwright Chromium on Railway
[providers]
node = {}

[phases.setup]
nixPkgs = ["nodejs"]

[phases.build]
cmds = [
  "npm install",
  "npx playwright install --with-deps chromium"
]
